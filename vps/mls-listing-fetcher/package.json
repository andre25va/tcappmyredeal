{
  "name": "mls-listing-fetcher",
  "version": "1.0.0",
  "description": "Heartland MLS listing data fetcher via Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18"
  }
}
